L = ['I did it all', 4, 'love']

# ordered sequence of values 순서가 있음
# tuple 은 mutable 수정이 가능함
#
for i in range(len(L)): #(0,1,2)
    print(L[i])

# objects of type

# + 와 append 에는 차이가 있음
# .append 는 list 안에 list 로 들어감
# + 는 list 와 list 의 병합, 새로운 list object

